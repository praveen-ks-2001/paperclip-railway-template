{
  "name": "paperclip-railway",
  "version": "1.0.0",
  "description": "Railway wrapper for paperclipai/paperclip with a web-based setup page",
  "private": true,
  "scripts": {
    "start": "node scripts/start.mjs",
    "postinstall": "node scripts/patch-paperclip-ui.mjs",
    "build": "node scripts/patch-paperclip-ui.mjs && echo 'No build step required'"
  },
  "dependencies": {
    "drizzle-orm": "0.41.0",
    "paperclipai": "2026.428.0"
  },
  "engines": {
    "node": ">=20"
  }
}
